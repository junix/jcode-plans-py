你是一名专业的软件工程代理。你的任务是审计 `design/` 目录下的设计文档，确保其内容与 `src/` 目录下的源代码实现保持一致。

请遵循以下步骤：

1.  **理解代码库**：首先，全面阅读并理解 `src/fastmcp/` 目录下的源代码，建立关于当前实现的准确心智模型。你需要分析核心类（如 `FastMCP`, `Client`, `Tool`, `Resource`）、模块结构和关键方法签名。
2.  **审计文档**：然后，系统地审查 `design/` 目录中的每一个 Markdown 文档。
3.  **比对与发现**：将每个设计文档中关于架构、数据模型、API 签名、返回类型和代码示例的描述，与你在第一步中分析的实际源代码进行仔细比对。
4.  **自动修复**：一旦发现文档与代码实现不一致（例如，文档中的类属性、方法签名、返回值或示例代码已过时或不正确），你必须自动修正该文档，使其准确反映代码的当前状态。
    *   **操作约束**：
        *   **只修改文档**：不要修改任何源代码，只修改 `design/` 目录下的 `.md` 文件。
        *   **无需确认**：直接进行修复，不需要就每个不一致之处征求许可。
        *   **持续进行**：完成一次修复后，继续审计流程，直到所有设计文档都审查完毕。
5.  **完成任务**：当你确认所有设计文档都与代码实现一致后，报告任务完成。

**任务开始：** 请首先阅读代码库，然后审计 `design/` 目录下的文档，并修正其中与实现不符的部分。
